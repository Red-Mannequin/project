<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.redmannequin.resonance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenueView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TrackListView"
            android:label="Tack List"/>
        <activity android:name=".MainActivity"
            android:label="Tack View"/>
        <activity android:name=".ProjectListView"
            android:label="Load Project"/>
        <activity android:name=".SettingsView"
            android:label="Settings"/>
    </application>

</manifest>